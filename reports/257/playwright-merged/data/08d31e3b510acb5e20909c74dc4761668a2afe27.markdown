# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: functional/InProgressTestAssignments.spec.ts >> Verify review mode for my 'In Progress' test assignments >> Validate different scenarios for 'In Progress' test assignment in review mode
- Location: src/tests/functional/InProgressTestAssignments.spec.ts:180:7

# Error details

```
Error: Test failures:
Time is up popup is not visible ...', 'Clicking on End Test button to get it
```

# Test source

```ts
  1  | export class RawValueLogger {
  2  |   private static enabled = false;
  3  | 
  4  |   static initialize(): void {
  5  |     RawValueLogger.enabled = process.env.LOG_RAW_VALUES === 'true';
  6  |   }
  7  | 
  8  |   private static serializeValue(value: any): string {
  9  |     if (value instanceof Map) {
  10 |       const obj: any = {};
  11 |       for (const [key, val] of value) {
  12 |         obj[key] = val;
  13 |       }
  14 |       return JSON.stringify(obj);
  15 |     }
  16 |     if (value instanceof Set) {
  17 |       return JSON.stringify(Array.from(value));
  18 |     }
  19 |     return JSON.stringify(value);
  20 |   }
  21 | 
  22 |   static log(messageOrName: string | any, value?: any): void {
  23 |     if (!RawValueLogger.enabled) return;
  24 |     if (value === undefined) {
  25 |       console.log(`[>->->: ${this.serializeValue(messageOrName)}  ]\n`);
  26 |     } else {
  27 |       console.log(`[>->->: ${messageOrName}: ${this.serializeValue(value)}  ]\n`);
  28 |     }
  29 |   }
  30 | }
  31 | 
  32 | // Initialize on import
  33 | RawValueLogger.initialize();
  34 | 
  35 | 
  36 | export class TestErrors {
  37 |     private static errors: string[] = [];
  38 | 
  39 |     static addError(error: string) {
  40 |         this.errors.push(error);
  41 |     }
  42 | 
  43 |     static checkErrors() {
  44 |         if (this.errors.length > 0) {
> 45 |             throw new Error('Test failures:\n' + this.errors.join('\n'));
     |                   ^ Error: Test failures:
  46 |         }
  47 |     }
  48 | }
```